# ------------------------------------------------------------------ #
# Lead Generation API — Phase 1 dependencies                         #
# Pin versions for reproducibility and security.                     #
# ------------------------------------------------------------------ #

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.1

# Database ORM (SQLite Phase 1, PostgreSQL Phase 2)
sqlalchemy>=2.0.30

# Config / validation
pydantic>=2.11.0
pydantic-settings>=2.6.0

# HTTP client (replaces requests — async-capable, better timeout control)
httpx>=0.27.0

# HTML parsing for enrichment
beautifulsoup4>=4.12.3
lxml>=5.3.0       # Faster parser backend for BeautifulSoup

# Form parsing (required by FastAPI for multipart, even if unused)
python-multipart>=0.0.9

# Phase 2 (commented out — uncomment when upgrading)
# psycopg2-binary==2.9.9   # PostgreSQL driver
# redis==5.0.4             # Redis client
# celery[redis]==5.4.0     # Task queue
